{
  "name": "run-pace-calculator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check script.js && node --check calculator.js",
    "verify:pages": "bash scripts/verify-pages-content.sh",
    "validate": "npm test && npm run check && bash -n scripts/verify-pages-content.sh"
  }
}
